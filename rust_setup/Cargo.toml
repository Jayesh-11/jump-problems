[package]
name = "app"
version = "0.1.0"
edition = "2024"

[[bin]]
name="two_sum"
path="two_sum.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

# serde_json is just for the example, not required in general
serde_json = "1.0"
